name: build

on: workflow_dispatch

jobs:
  build:
      runs-on: ubuntu-latest
      steps:
        - name: running 
          run: |
              git clone https://github.com/Col-E/Recaf.git
              cd Recaf
              chmod +x gradlew 
              ./gradlew build
          
