name:  Run

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: macos-latest
    
    
    steps:
      - name: Update 
        # We use --sync to get the latest package definitions
        run: brew install fastfetch 

      
      - name: Run Fastfetch
        run: fastfetch
        
