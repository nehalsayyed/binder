name: Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: running
        run: |
          sudo apt update -y && sudo apt install neofetch -y && neofetch
          
