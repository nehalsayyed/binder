name: Run

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ubuntu:26.04
    steps:
      - name: Running
        run: apt update && apt install fastfetch -y && fastfetch
