name: Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: running
        run: |
          winget install -e --id Fastfetch-cli.Fastfetch
          fastfetch
          
