name: Manual Build

on:
  workflow_dispatch: # Allows manual trigger

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: gradle:jdk21

    steps:
       - run: |
            git clone https://github.com/gradle/gradle.git && cd gradle && chmod +x gradlew && ./gradlew 
            

      
