name: Build

on:
  workflow_dispatch:   # manual trigger

jobs:
  build:
    runs-on: ubuntu-latest 
    container:
      image: ubuntu:26.04

    steps:
      - name: Setup Environment
        run: |
          apt update -y
          apt install fastfetch -y
          fastfetch
