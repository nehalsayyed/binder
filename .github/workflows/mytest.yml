name: run

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ubuntu:26.04
    steps:
      - name: run
        run: |
          apt-get update
          apt-get install -y fastfetch
          fastfetch
