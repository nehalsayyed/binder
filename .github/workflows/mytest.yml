name: Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Install Fastfetch
        run: winget install -e --id Fastfetch-cli.Fastfetch

      - name: Run Fastfetch
        run: fastfetch
        
