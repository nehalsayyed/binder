name: FreeBSD CI

on:
  workflow_dispatch:

jobs:
  freebsd:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run in FreeBSD VM
        uses: vmactions/freebsd-vm@v0
        with:
          run: |
            uname -a
            pkg install -y tree
