name: nehal

on:
  workflow_dispatch:

jobs:
  build:
     runs-on: ubuntu-latest

     steps:
       - name: run
         run: |
           lspci
           lsusb
           netstat
           
       
